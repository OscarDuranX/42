#include <stdio.h>
#include <string.h>

int	ft_strlen(char *c)
{
	int	i;

	i = -1;
	while (c[++i])
	{
	}
	return (i);
}

/*int main() {
    char miCadena[] = "";
    size_t longitud; // Usar size_t para almacenar el resultado

    longitud = ft_strlen(miCadena); // Calcular la longitud

    printf("La cadena \"%s\" tiene %zu caracteres.\n", miCadena, longitud); // Salida: 11

    return 0;
}*/
